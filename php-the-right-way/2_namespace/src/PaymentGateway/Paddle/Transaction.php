<?php

declare(strict_types=1);

namespace PaymentGateway\Paddle;

// use DateTime;
// use Notification\Email;

class Transaction
{
    public function __construct()
    {
        // var_dump(new CustomerProfile());
        // var_dump(new \DateTime()); // looks for class in the global namespace
        // var_dump(new DateTime());

        // var_dump(new Email());

        // var_dump(explode(',', 'hello, world')); // uses local function
        // var_dump(\explode(',', 'hello, world')); // uses php's built-in function
    }
}

// function explode($separator, $str): string
// {
//     return 'foo';
// }
